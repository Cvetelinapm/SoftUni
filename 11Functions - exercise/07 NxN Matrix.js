function solve(num){
    for (let i = 0; i < num; i++) {
        let output = '';
        for (let k = 0; k < num ; k++) {
            const element = num;
            output += element + ' ';
        }
        console.log(output);
    }
}
solve(5)